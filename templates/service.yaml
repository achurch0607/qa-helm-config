apiVersion: v1
kind: Service
metadata:
  name: {{ include "ac-http.fullname" . }}
  labels:
    {{- include "ac-http.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ac-http.selectorLabels" . | nindent 4 }}
